releasePrefix: "-"

defaultImage: ""
defaultImageTag: ""

generic:
  usePredefinedAffinity: false
  extraVolumeMounts:
    - name: vault-inject
      mountPath: "/var/run/secrets/app"
      readOnly: false
  extraVolumes:
    - name: vault-inject
      emptyDir: { }