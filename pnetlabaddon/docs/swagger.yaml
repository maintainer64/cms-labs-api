basePath: /pnet-lab-addon/api
info:
  contact:
    email: your@mail.com
    name: API Support
  description: This is an auto-generated API Docs.
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  termsOfService: http://swagger.io/terms/
  title: API
  version: "1.0"
paths:
  /v1/sso/login:
    get:
      consumes:
      - application/json
      description: Отправка пользователя с параметрами на сервер аутентификации.
      parameters:
      - description: Дополнительные параметры в base64
        in: query
        name: extra
        type: string
      - description: Путь с которого выполнился редирект
        in: query
        name: path
        type: string
      produces:
      - application/json
      responses:
        "307":
          description: Temporary Redirect
      summary: Отправка пользователя с параметрами на сервер аутентификации.
      tags:
      - SSO
  /v1/sso/openid:
    get:
      consumes:
      - application/json
      description: 'Дополнительные параметры:'
      parameters:
      - description: ID запроса по всему пути аутентификации
        in: query
        name: state
        type: string
      - description: Оригинальный путь по всему пути аутентификации
        in: query
        name: path
        type: string
      - description: Код OTP обмена авторизации
        in: query
        name: code
        type: string
      - description: ClientID запрашиваемого из аутентификации
        in: query
        name: application
        type: string
      - description: Дополнительные данные с backend base64 по всему пути аутентификации
        in: query
        name: extra
        type: string
      produces:
      - application/json
      responses:
        "307":
          description: Temporary Redirect
      summary: Второй фактор openid.
      tags:
      - SSO
securityDefinitions:
  ApiKeyAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
